spring:
    config:
        import: security-variable.yml
    application:
        name: Memo
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:myspl://localhost:3306/memo-project
        username: root
        password: ${mysql.password}